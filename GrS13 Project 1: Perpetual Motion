import turtle
screen=turtle.Screen()
t = turtle.Turtle()
t.speed(0)
def turnLeft():
  t.left(5)
def turnRight():
  t.right(5)
screen.onkey(turnLeft,"left")
screen.onkey(turnRight,"Right")
screen.listen()
while True:
  t.forward(1)
